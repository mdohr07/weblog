---
export interface Props {
  post: any;
}

const { post } = Astro.props;
---

<article class="post-preview">
    <a href={post.url}>
    {post.image && <img width="720" height="420" class="post-main__img postimg" loading="lazy" src={post.image} alt={post.alt} />}
    <h1 class="title">{post.title}</h1>
    <time class="publish-date">{post.publishDate}</time>
    </a>
</article>

<style>
  a {
    text-decoration: none;
  }

  a:hover > .title {
    text-decoration: underline;
  }

.post-preview {
margin-bottom: 2rem;
}

.post-main__img {
  aspect-ratio: 16 / 9;
  object-fit: cover;
  width: 300px;
  max-width: 600px;
  height: auto;
  margin-bottom: 1rem;
}

.title,
.author,
.publish-date {
  margin: 0;
}

.publish-date,
.author {
  font-size: 1rem;
  color: var(--theme-text-lighter);
}

.title {
  font-size: 2.25rem;
  font-weight: 700;
  color: var(--theme-text);
}

</style>
